# IO_descriptors.yaml example

`example_IO_descriptors.yaml` is copied from a typical Isaac Lab export and is provided as a reference.

The stack reads:
- observations from `observations.policy` in order
- actions from the first element of `actions`

If your YAML differs, update parsers accordingly.
